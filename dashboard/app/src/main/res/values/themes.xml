<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose drives the in-app look; this base theme keeps the system surfaces dark. -->
    <style name="Theme.OdsDashboard" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@color/charcoal</item>
        <item name="android:navigationBarColor">@color/charcoal</item>
        <item name="android:windowBackground">@color/charcoal</item>
    </style>

    <!-- No-UI theme for the launcher-grid trampoline: it starts a browser and finishes,
         so it must never flash a window of its own. -->
    <style name="Theme.OdsDashboard.Invisible" parent="android:Theme.Translucent.NoTitleBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoDisplay">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
